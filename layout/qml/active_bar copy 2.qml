import QtQuick
import QtQuick.Controls

Item {
    id: root
    anchors.fill: parent

    Column {
        // id: buttonColumn
        // anchors.fill: parent
        // anchors.topMargin: 8
        // anchors.bottomMargin: 8
        // spacing: 4

        // Repeater {
        //     model: backend.icon_keys

        //     delegate: Button {
        //         width: parent.width
        //         height: width

        //         flat: true

        //         icon.source: backend.icons[modelData]
        //         icon.width: Math.round(parent.width * 0.6)
        //         icon.height: Math.round(parent.width * 0.6)
        //         icon.color: "transparent"

        //         onClicked: backend.trigger_clicked(modelData)
        //     }
        // }
    }
}
